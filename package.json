{
  "name": "salaryapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "/SalaryApp",
  "scripts": {
    "dev": "php -S localhost:8000 -t .",
    "build": "echo 'No build step needed for vanilla JS'",
    "lint": "echo 'No linting configured'",
    "preview": "php -S localhost:8000 -t ."
  },
  "dependencies": {},
  "devDependencies": {},
  "description": "Salary App - Vanilla JavaScript with PHP backend"
}
